/* 
    Scope of a variable is the region in code where the existence of varable is valid.
    Based on scope, variable can be classified into two types:
    1. Local variables
    2. Global Variable 
    .Local Variable: Local variables are declared inside the brackets of any
                     function and can be accessed only from there.
    
    Global Variable: Global variables are declared outside any function and
                     can be accessed from anywhere.
                     
 */
